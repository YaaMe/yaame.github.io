# @fedify/cfworkers pins @cloudflare/workers-types to ^4 while wrangler wants
# ^5. It is a types-only package — nothing about it reaches the runtime — so the
# conflict is resolved rather than worked around in each install command.
legacy-peer-deps=true
