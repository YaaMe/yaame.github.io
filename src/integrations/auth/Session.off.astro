---
/**
 * The static profile's session badge: nothing.
 *
 * Chosen by the alias in astro.config.mjs, not by a condition at render time.
 * A `server:defer` anywhere in the module graph makes the build emit a server
 * entry, and that profile's whole claim is that it has none.
 */
---
